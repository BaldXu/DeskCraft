<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 监控组件按配置间隔刷新；Android 12+ 需要该权限才能精确闹钟，
         未授予时 Provider 自动回落 setAndAllowWhileIdle（非精确，Doze 节流）。 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- 日历组件读取系统日历事件（节日/纪念日，零维护数据源），运行时申请。 -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <application
        android:label="DeskCraft"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 数字时钟桌面组件（M1 全链路验证） -->
        <receiver
            android:name=".ClockWidgetProvider"
            android:exported="false"
            android:label="数字时钟">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/clock_widget_info" />
        </receiver>

        <!-- 系统监控桌面组件（M4）：定时刷新由 AlarmManager 指回本 receiver -->
        <receiver
            android:name=".MonitorWidgetProvider"
            android:exported="false"
            android:label="系统监控">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.deskcraft.desk_craft.MONITOR_REFRESH" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/monitor_widget_info" />
        </receiver>

        <!-- 日历桌面组件（V1）：位图渲染 + 系统日历事件 + 翻日即时更新。
             监听日期/时区变更与日历数据变更，另有午夜兜底闹钟。 -->
        <receiver
            android:name=".CalendarWidgetProvider"
            android:exported="false"
            android:label="日历">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.deskcraft.desk_craft.CALENDAR_REFRESH" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.PROVIDER_CHANGED" />
                <data
                    android:host="com.android.calendar"
                    android:scheme="content" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/calendar_widget_info" />
        </receiver>

        <!-- 自定义组件桌面组件（M6）：展示 Flutter 图层画布渲染的位图，
             AlarmManager 周期采样（写入变量快照）后经 home_widget 后台链路重渲染 -->
        <receiver
            android:name=".CustomWidgetProvider"
            android:exported="false"
            android:label="自定义组件">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.deskcraft.desk_craft.CUSTOM_WIDGET_REFRESH" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/custom_widget_info" />
        </receiver>

        <!-- home_widget 后台回调入口（M6）：接收 HomeWidgetBackgroundIntent.getBroadcast，
             拉起 headless Flutter 引擎执行 Dart 侧顶层回调（重渲染位图） -->
        <receiver
            android:name="es.antonborri.home_widget.HomeWidgetBackgroundReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.BACKGROUND" />
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
